{% extends "node--full.html.twig" %}

{% set _zipcode = node.field_zip_code.target_id %}
{% set _auction_status = content.field_property_auction_status.0['#markup'] %}

{% block node_hero %}
  {%
    set heroData = {
      'hero' : {
        'subtitle': content.field_subtitle,
        'title': content.field_hero_title|field_value ? content.field_hero_title : label,
        'image': content.field_image,
        'background_image': content.field_background_image,
      }
    }
  %}
  {% include directory ~ '/templates/_includes/components/hero--home.html.twig' with heroData %}

  {{ _local_actions_block }}
  {{ _primary_local_tasks_block }}

{% endblock %}